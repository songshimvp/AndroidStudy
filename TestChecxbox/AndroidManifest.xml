<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="songshi.testchecxbox"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="songshi.testchecxbox.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="CheckBoxActivity"></activity>
        <activity android:name="RadioGroupActivity"></activity>
        <activity android:name="MySpinner"></activity>
        <activity android:name="MyGridView"></activity>
        <activity android:name="songshi.testchecxbox.ImageSwitchActivity"></activity>
        <activity android:name="ProgressBarActivity"></activity>
        <activity android:name="EdittextActivity"></activity>
    </application>

</manifest>
